/**
 * @file
 * Javascript functionality for the UNHCR Suite Extras administration UI.
 */
(function ($) {
	$( 'document').ready( node_preprocess );
	function node_preprocess( jQuery ){
		$("label[for$=yes-no-und]").text('');
		};
	});